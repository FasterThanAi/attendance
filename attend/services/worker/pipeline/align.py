"""Stub for Phase 0. Implemented in the phase that owns this stage --
see the roadmap PDF's phase-by-phase breakdown (Phase 3: extract/detect,
Phase 4: quality/align, Phase 5: embed/cluster, Phase 6: match).

Every pipeline stage must end up a pure function: typed input, typed output,
independently testable without a database, network, or FastAPI (non-negotiable
rule #1 in the global brief). Nothing here yet on purpose.
"""
