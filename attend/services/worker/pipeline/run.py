"""Stub for Phase 0.

The job orchestrator (`process_session(job_id)`, the single RQ entrypoint
that runs every pipeline stage in order and manages per-stage artifact
caching under /data/jobs/{job_id}/) is a Phase 2 deliverable. Nothing here
yet on purpose.
"""
